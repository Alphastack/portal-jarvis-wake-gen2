# Contributing

Use Kotlin, keep Android I/O at the edge, and add a failing JVM test before changing core wake policy. Run `./gradlew test lint assembleDebug` before a pull request. Contributions are licensed Apache-2.0.
